# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# sanity studio build output — rebuilt from source by `sanity deploy`,
# so the hosted studio never reads this. Vite re-hashes every chunk on each
# build, which made every deploy show up as dozens of file churn in git.
/dist

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/*
!.vscode/extensions.json
.idea
